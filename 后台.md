# Admin 后台登录问题

## 问题描述

访问 `/admin/` 输入密码后无法登录，什么密码都不行。表单提交后页面只是刷新，没有实际验证。

## 根本原因

项目 `astro.config.mjs` 中 `output` 为 `"static"`（默认值），所有 `.astro` 页面在构建时就固定为静态 HTML。页面中的服务端逻辑（`Astro.cookies`、`Astro.redirect()`、`Astro.request`）在客户端访问时不会执行。

当前 `/admin/index.astro` 中的密码验证、cookie 设置、重定向代码全部失效，因为它们只在构建时运行一次（此时无 cookie，`authed` 永远为 `false`）。

## 解决方案

把 `astro.config.mjs` 中的 `output` 改为 `"server"` 或 `"hybrid"`，使 `.astro` 页面以 SSR 模式运行。

### 方案一：改为 server 模式（推荐）

```js
// astro.config.mjs
export default defineConfig({
  output: "server",
  // ...其他配置
});
```

所有页面默认走 SSR，适合需要动态功能的博客。

### 方案二：改为 hybrid 模式

```js
// astro.config.mjs
export default defineConfig({
  output: "hybrid",
  // ...其他配置
});
```

默认静态，需要 SSR 的页面加 `export const prerender = false;`。更灵活但需要逐页配置。

### 部署注意

- **Vercel**：需要 `@astrojs/adapter-node` 或 `@astrojs/adapter-vercel`，当前项目已有 `@astrojs/cloudflare` 适配器
- 如果部署到 Cloudflare Workers，SSR 模式也兼容
- 改为 SSR 后，`pnpm build` 的输出会从纯静态变为混合模式

## 当前 Admin 页面逻辑（供参考）

文件：`src/pages/admin/index.astro`

- 密码：`wuJIA1130`
- 登录方式：GET 参数 `?pwd=xxx`（绕过 Vercel POST 405）
- Cookie：`admin_auth`，有效期 24 小时
- 登出：访问 `/admin/?logout=1`
- 登录后通过 `<AdminShell>` Svelte 组件显示管理后台
